测试结果：

好消息：
- 可以看到所有row了，且渲染正确
- 在大部份区域移动光标，都能正确的进入/退出editMode

坏消息：我按下键，到了scrollMargin的地方时，不是太正常

Bug1：
- 比如我在sample.md里的line42，这是个空行，当时正好是scrollMargin的地方
- 我按下，正确的scrollup 1 row了，这个非常好。
- 于是光标就处在了line43。line43是个“```go”
    - 按理说当光标进入line43的时候，它应该进入editMode
    - 但是没有，还保持着render状态。
    - 直到我再次按下键进入了line44之后，line43才和它下面的lines一起变成editMode

Bug2：
- line55-59是个table
- 我光标处在其中的某line，所以整个table是编辑状态，这很好
- 我按下键，把光标从line57移到line58的时候，正确触发了scrollup
    - 但是，scrollup了2 rows。正确的应该是 1 row
    - 于是我的光标就落在了line60了，但是statusLine却显示光标是58
    - 整个table仍保持在editMode
- 我继续按下键
    - statusLine显示光标到59了
    - 我的光标自动向上移了一下，真的回到line59了
- 我继续按下键
    - statusLine显示光标到60了
    - 因为光标脱离了table，所以整个table进入了渲染展开。于是整体向上scroll了，整个表格渲染后给下方留出了scrollmargin的空间。我是认为位置计算的是正确的
    - 但是光标不见了，没有停留在line60上面，而是向下跑到statusLine的下边去了，停在了infoPane上面。虽然statusLine是显示的60
- 我继续按下键
    - 光标正确的跑到了line61
    - 整体也正确的scrollup 1 row
    - statusLine显示光标在61
    - 一切又正常了

我看你需要加些更具体的log了。哈哈
