
我在想，当我在使用 micro 编辑一个文件 fileA的时候，能不能通过某个键，打开一个浮动的窗口notePane，我可以在里面记下一些我的想法。
- notePane在fileA的当前光标的下面出现，左右充满当前的编辑窗口，高度可以固定5row
- notePane直接覆盖在fileA的前面，并且冻结fileA的所有功能，或者叫 unfocus fileA edit window
- notePane里，有完整的microNeo的编辑功能，内容保存在一个固定名字的md文件里
- 这个浮窗不要micro现有的statusLine,它的边框就是一个像表格的外框那样的ascii组成的外框

# 下面我们完善一下位置计算

## 正常时候

notePane应当出现在当前光标的下面的row里
- 比如当前光标在row5，注意不是Line5，是row5，则notePane的边框从row6开始，内容从row7开始
- 如果当前有多光标，则从最下面的那个光标的下一个row开始
- 如果当前有select，则从select区域的最下面的下一个row开始

如果下方的空间不够了
- 因为用户肯定是对着上面的文字思考并写下notePane里的注解和意见，所以上面的内容是重要的
- 如果下面的空间不够放notePane了，
	- 假设notePane的内容是5row，加上边框需要7row
	- 就把原始的文件编辑pane里面的内容向上滚动，让光标与statusLine之间有7row的空间
