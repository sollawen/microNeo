# Runtime files for Micro

This directory will be embedded in the Go binary for portability, but it may just as well be put in `~/.config/microNeo`. If you would like to make your own colorschemes
and syntax files, you can put them in `~/.config/microNeo/colorschemes` and `~/.config/microNeo/syntax` respectively.

